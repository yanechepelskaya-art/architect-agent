# BUILD_LOG — Architect Agent

**Цель:** журнал постройки. Воспроизводимость. Хронология этапов.
**Формат:** Дата / Файлы / Что / Решение / Результат.
**Принцип:** только факты. Без оценок.

---

## ЭТАП 0. ФУНДАМЕНТ

**Файлы:** `agent_light.py`, `auto_watch.py`, `collect_data.py`

**Что сделано:**
- Telegram-бот с 17+ кнопками анализа.
- Автономный режим `auto_watch.py` на Render 24/7.
- Сбор данных `collect_data.py` — цена, объём, OI, funding, delta.
- Деплой: GitHub + Render, три сервиса.

**Решение:** собственная система вместо чужой. Не копировать инфраструктуру фондов. Копировать дисциплину.

**Результат:** работает в проде. Бот отвечает.

---

## ЭТАП 1. ДАТАСЕТЫ И БЭКТЕСТ

**Файлы:** `btc_data_features.csv`, `btc_data_labeled.csv`, `training_data.csv`, `training_data_clean.csv`, `backtest_aplus.py`, `backtest_combos.py`

**Что сделано:**
- Фичи: ATR, RSI, hour, price_change_5, oi_change_5.
- Метки: фазы (compression, impulse, emanation, flush).
- Бэктест с комиссиями и slippage.
- Walk-forward по фазам.

**Решение:** проверять edge до торговли.

**Результат:** фазы убыточны на out-of-sample. Walk-forward показал:
- impulse: train -0.239% → test -0.355%
- compression: train -0.278% → test -0.314%
- emanation, flush — нет данных в test.

---

## ЭТАП 2. ДИАГНОЗ

**Файлы:** те же.

**Что обнаружено:**
- Фазы убыточны на OOS.
- A+ score 4 убыточен (15.4% winrate, -0.275%).
- A+ score 6-7 прибыльны (57%, 71%), но мало сигналов (35 на train).
- ML — синтетика. 99.63% точности = overfit.
- На test — score 6-7 не появились.

**Решение:** убрать ML из блокеров. Поднять порог A+.

**Результат:** edge не доказан. Ни на одном сигнале.

---

## ЭТАП 3. ПРАВКА A+

**Файлы:** `agent_light.py`

**Что сделано:**
- ML убран из блокеров (`if False`, строка 2506).
- ML убран из подсчёта score (`ML —`).
- Порог A+ в бэктесте: 4 → 6.
- Тексты в агенте: `/9`, `/10` → `/7`.
- Логирование A+ — только при `score_layers >= 6`.

**Решение:** A+ = 7 слоёв, порог 6/7. Бэктест должен совпадать с агентом.

**Результат:** A+ в проде — 7 слоёв, порог 6/7. Коммиты: 7d1542a, 9c13717.

---

## ЭТАП 4. ФИКС ПУТИ CSV

**Файлы:** `agent_light.py`, `training_data_snapshot.csv`

**Что сделано:**
- `training_data_snapshot.csv` — залит в репо.
- Путь упрощён: только `_P(__file__).parent / "training_data_snapshot.csv"`.
- Убран fallback на несуществующий путь.

**Решение:** файл лежит рядом со скриптом. Всегда берётся оттуда.

**Результат:** A+ в проде показывает X/7, не "нет данных". Проверено: 2/7. Коммиты: 7ed129c, 46a290e.

---

## ЭТАП 5. ТОКЕН И БЕЗОПАСНОСТЬ

**Файлы:** `agent_light.py`, `auto_watch.py`

**Что сделано:**
- Webhook проверен — чист.
- Старый токен AAEyfZ — отозван. Мёртв (401).
- Новый токен AAEXdg5 — получен.
- Хардкод заменён на `os.getenv("BOT_TOKEN") or "fallback"`.
- `import os` — добавлен.

**Решение:** токен в env. В коде — fallback. Убрать из кода.

**Результат:** код читает из env. Бот работает. Коммит: b4a3b7c.

**Осталось:** третий revoke (AAEXdg5 светился в чате). Render env — проверить.

---

## ЭТАП 6. РАЗБОР 11 ОКТЯБРЯ 2025

**Что сделано:**
- Хронология каскада 10-12 октября.
- Структурные слои:
  - Перегрев лонгами (funding 30%).
  - USDe-петля (5x плечо).
  - Единая точка отказа Binance.
  - Окно уязвимости (фикс 14 октября).
  - Кит на Hyperliquid ($1.1 млрд шорт).
- Триггер vs структура — разделены.

**Решение:** правильный язык — структурный каскад. Не аномалия, не чёрный лебедь.

**Результат:** понято, почему Женя не увидел. Гипотеза о слоях предупреждения.

---

## ЭТАП 7. ПЛАНИРОВАНИЕ

**Что сделано:**
- Sleeve-дисциплина.
- Bias-аудит ML.
- Execution cost check.
- Слои предупреждения (funding, USDe, WLFI, анонсы бирж, Hyperliquid, макро).
- Трекер структурных каскадов — модуль + кнопка.
- Исторические разборы: FTX, COVID, Китай.
- Методология фондов.

**Решение:** строить систему, которая видит сигналы заранее. Не копировать инфраструктуру фондов. Копировать дисциплину.

**Результат:** план — 34 пункта, 13 правил.

---

## ПРАВИЛА

1. Нет сигнала — нет сделки. Без "если хочешь".
2. Оценки — только по числам.
3. Sleeve с отрицательным expectancy → disabled.
4. Проверять числа, не оценки.
5. Раз в неделю: есть сигнал с OOS+? Да/нет.
6. Критерий 2-3 месяца — один доказанный сигнал.
7. Не копировать инфраструктуру фондов. Копировать дисциплину.
8. Каждый слой предупреждения — отдельный sleeve.
9. Слой без данных — не слой.
10. Не предсказывать. Видеть.
11. Доводить до конца. Одно за раз.
12. Документировать сразу.
13. Фокус на edge.
