services:
  - type: worker
    name: architect-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 architect.py
    plan: starter
    
  - type: web
    name: architect-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 dashboard.py
    plan: starter
